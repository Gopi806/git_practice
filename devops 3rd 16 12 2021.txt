Working on containers

=========================
1. To see the list of running containers
   docker container ls
2. To see the list of all the containers (Running ans stopped)
   docker ps -a
3. To stop a running container
   docker stop container_name/container_id

4. To start a stopped container
   docker start

logs waste datadocker rm -f $$

To delete docker all images use
docker system prune -af


sudo su -

yaml is file format


Multi Container Architecture (Class 4th 17/Dec/2021) 
=============================

1. -- link run command option (Depricated) means outdated.
2. docker-compose
3. docker networking

4. python scripts
5. Ansible playbooks


To come out of a container without exit
Ctrl+p, ctrl+q
